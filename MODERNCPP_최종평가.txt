
1.	NULL POINTER 를 나타내는 리터럴은 무엇인가요 ?


2.	다음중 에러는 ? ( int n = 10 일때)
①	const int c1 = 10;
②	const int c2 = n;
③	constexpr int c1 = 10;
④	constexpr int c2 = n;


3.	복사 생성자가 아닌 이동 생성자를 호출하기 위해 사용하는 C++ 표준 함수는 ?


4.	아래 코드에서 auto 는 각각 무슨 타입으로 결정 될까요 ?
int x[3] = {1,2,3};
auto a1 = x;      // auto 의 타입은 ? 
auto& a2 = x;    // auto 의 타입은 ?


5.	T 가 포인터 인지 알아내는 코드를 작성해 보세요
   If (          )   
       cout << “T는 포인터” << endl;


6.	함수 템플릿 사용시 치환에 실패하면 에러가 아니라 C++ 코드를 생성하지 않는다라는 것을 나타내는 용어는 ?





7.	아래 코드에서 T와 a의 타입을 각각 적어 보세요
template<typename T> void foo(T&& a) {}

int main()
{
	int n = 10;
	foo(n);  // T의 타입 :        a의 타입 : 
	foo(10); // T의 타입 :        a의 타입 : 
}

8.	Forwarding reference 로 전달 받은 인자를 다른 곳으로 전달할 때 완벽한 전달을 위해 사용하는 C++ 표준 함수의 이름은 무엇인가요 ?


9.	 객체 초기화시에 직접 초기화만 가능하고 복사 초기화는 사용할 수 없도록 하기위해 생성자 앞에 붙이는 키워드는 무엇인가요 ?


10.	다음 설명중 틀린 것은 ?
①	람다표현식의 결과는 rvalue 이다.
②	람다 표현식은 함수 객체를 만드는 표현식이다.
③	mutable 람다는 값으로 캡쳐된 지역변수의 원본을 수정할 수 있다.
④	복사 생성자를 사용할 수 없게 하려면 함수를 삭제 하면 된다.

